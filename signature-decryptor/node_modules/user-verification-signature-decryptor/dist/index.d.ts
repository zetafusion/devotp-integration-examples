export interface DecryptedPayload {
    identifier: string;
    createdAt: string;
}
export interface ValidationResult {
    isValid: boolean;
    decryptedData?: DecryptedPayload;
}
export declare class PayloadDecryptor {
    private readonly clientKey;
    constructor(clientKey: string);
    private decryptPayload;
    validateDecryption(email: string, signature: string): Promise<ValidationResult>;
}
